#include<stdio.h>

int main (){
	int age; 
	printf("MOi ban nhap so :");
	scanf("%d",&age);
	
	
	if( age % 2 == 0){
		printf("so chan :"); 
	} else
		printf(" so le : ");
	
	return 0; 
} 
